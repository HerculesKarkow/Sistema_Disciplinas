@model IEnumerable<DisciplinasFaculdade.Models.Sala>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Predio.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QtdLugares)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Predio.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QtdLugares)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdSala }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdSala }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdSala })
        </td>
    </tr>
}

</table>
